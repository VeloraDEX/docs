---
title: "Market → Delta (parallel)"
description: "Keep your Market integration wired, opt into Delta intents alongside it, and branch on the response shape."
keywords: ["migration","delta","market","parallel"]
---

## What breaks if you do nothing

Nothing. This migration is opt-in. Your existing Market integration keeps working as-is.

## Why migrate

- **Better UX** — Delta intents are gasless and MEV-protected. Users sign once; the solver network fills.
- **No code rewrite** — `GET /quote` with `mode=ALL` returns either a Market `market` block (same shape as `/prices`'s `priceRoute`) or a Delta `delta` block — Velora picks the winner server-side and you branch on which block is present. See [Trading modes](/integrate/trading-modes).

## Steps

1. Switch from `GET /prices` to `GET /quote?mode=ALL`.
2. Branch on the response: if `response.delta` exists, take the gasless path; else fall back to `response.market` and the existing calldata flow (it's the same shape your code already handles).
3. For the Delta path, call `POST /delta/orders/build`, sign the EIP-712 typed data, then `POST /delta/orders`.

## End-state check

Run a quote with `mode=ALL`. Confirm the response carries either a `market` or `delta` block (the server-picked winner) and complete a swap end-to-end.
