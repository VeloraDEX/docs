{
  "name": "velora-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "mint dev",
    "build": "mint dev --port 0",
    "check": "mint broken-links"
  },
  "devDependencies": {
    "mint": "^4.2.559"
  }
}
